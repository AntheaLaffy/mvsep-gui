# Maintainer: AntheaLaffy <anthealaffy@gmail.com>

pkgname=mvsep-gui
pkgver=1.0.0
pkgrel=1
pkgdesc="MVSEP Music Separation GUI - Modern and beautiful music separation tool"
arch=('x86_64')
url="https://github.com/AntheaLaffy/mvsep-gui"
license=('Apache')
depends=('python>=3.8' 'python-pyqt6' 'python-mvsep-cli')
makedepends=()
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=

source=("${url}/archive/refs/tags/v${pkgver}.tar.gz")
noextract=()

sha256sums=('SKIP')

package() {
    pip install "$srcdir/mvsep-gui-${pkgver}" --root="$pkgdir" --prefix=/usr --compile
}
